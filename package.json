{
  "name": "singularity",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm -r dev",
    "build": "pnpm -r build",
    "control-plane": "pnpm --filter @singularity/control-plane dev",
    "ui": "pnpm --filter @singularity/ui dev",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  },
  "dependencies": {
    "commonmark": "^0.31.2",
    "get-stream": "^9.0.1",
    "pdfkit": "^0.17.2",
    "pdfkit-commonmark": "^1.2.0"
  }
}
