# Singularity Agent - Python Dependencies

# PyTorch CPU-only (must be before sentence-transformers to avoid CUDA download)
--extra-index-url https://download.pytorch.org/whl/cpu
torch

# MCP server for exposing tools to Claude
mcp>=1.0.0

# Vector search with sentence transformers (local embeddings)
sentence-transformers>=2.2.0

# Tokenization for accurate chunking
tiktoken>=0.5.0

# SQLite vector extension (optional, falls back to manual similarity)
# sqlite-vec>=0.1.0

# Standard library sqlite3 is used for database
